services:
  - type: web
    name: pdf-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn PDF:app --bind 0.0.0.0:10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
